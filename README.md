# Proteus

Simulation framework for mechanism-level market structure experiments.

## Development

Install dependencies:

```bash
poetry install
```

Run tests:

```bash
poetry run pytest
```

Lint:

```bash
poetry run ruff check .
```

Format:

```bash
poetry run ruff format .
```
